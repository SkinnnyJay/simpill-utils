/**
 * Shared types for @simpill/react.utils.
 * Re-exports of React types.
 */

import type { ComponentType, ReactNode, RefObject } from "react";

export type { ComponentType, ReactNode, RefObject };
