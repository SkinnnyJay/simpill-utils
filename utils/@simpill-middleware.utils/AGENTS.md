# middleware.utils

- Depends on `@simpill/request-context.utils`, `@simpill/uuid.utils`.
- Layout: shared (types), server (createCorrelationMiddleware), client (types only).
- Tests: unit test with mock req/res/next and getRequestContext; 80%+ coverage.
