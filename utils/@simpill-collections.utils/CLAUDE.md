# @simpill/collections.utils

Data structures package. All implementations are in `src/shared/collections/`. Tests live in `__tests__/shared/unit/`. Run `npm test`, `npm run test:coverage`, `npm run build`, `npm run verify` from the package root.
