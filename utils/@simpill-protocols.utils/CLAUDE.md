# protocols.utils

TypeScript package exporting shared protocols (HTTP, correlation headers, env boolean parsing, log env keys). Structure: `src/shared/` plus client/server re-exports. Commands: `npm run build`, `npm test`, `npm run verify`.
