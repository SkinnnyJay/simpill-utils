/** Server constants for nextjs.utils (literal audit). */

/** Default validation error message when Zod issues have no message. */
export const ERROR_VALIDATION_FAILED = "Validation failed" as const;
/** Error code for validation failures. */
export const ERROR_CODE_VALIDATION = "VALIDATION_ERROR" as const;
/** Default error code for server errors. */
export const ERROR_CODE_SERVER = "SERVER_ERROR" as const;
