# @simpill/file.utils

- **Server**: readFileUtf8, readFileJson, readFileAsync, writeFileUtf8, writeFileJson, writeFileAsync, ensureDir (Node fs.promises).
- **Client / Shared**: No implementation; file I/O is server-only.
