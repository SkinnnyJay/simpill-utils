/** Shared constants for file.utils (literal audit). */
export const VALUE_1 = 1;

/** Path outside root (append filePath). */
export const ERROR_PATH_RESOLVES_OUTSIDE_ROOT_PREFIX = "Path resolves outside root: " as const;
