# @simpill/file.utils

Package for typed file I/O: readFileUtf8, readFileJson, writeFileUtf8, writeFileJson, ensureDir. Server-only (Node).

- **Server**: `@simpill/file.utils/server` – all file helpers.
